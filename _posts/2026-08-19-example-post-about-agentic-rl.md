---
layout: post
title: Example Post About Agentic RL
date: 2026-08-19 10:00:00
description: A short placeholder note used to check Markdown, headings, and the left table of contents.
tags: [notes]
giscus_comments: false
related_posts: false
toc:
  sidebar: left
---

This is a minimal long-form Markdown post. It exists so the blog list, article layout, and left table of contents can be checked.

## Introduction

The article itself should stay the visual center. The left sidebar should list the headings below and stay visible while scrolling on desktop.

Agentic reinforcement learning is used here only as a placeholder topic. The point of this page is structure, not content.

## Background

A technical note usually needs a short setup before the details.

Useful Markdown checks:

- A [plain link](https://github.com/alshedivat/al-folio)
- **Bold** and *italic* text
- `inline code`
- A short quotation

> Keep the page quiet. The writing should be easier to read than the chrome around it.

## Method

The method section is only here to create another heading for the table of contents.

### Data

Pretend there is a small dataset and a training loop. No real experiment is described.

### Training loop

The loop would collect trajectories, compute a reward, and update a policy. That sentence is enough for this placeholder.

## Experiment

A later post can replace this section with an actual write-up. For now it only needs to be long enough to make the page scroll.

Line after line of notes would normally live here. The sidebar should remain usable while the main column stays the reading surface.

Another paragraph exists only so the page is taller than the viewport. If the table of contents is working, clicking **Conclusion** should jump to the last section.

## Conclusion

This post is a layout fixture:

1. Markdown renders.
2. Headings appear in the left TOC.
3. The article stays the main object on the page.

Replace it when real writing starts.
